import StyledLoadingMoreSpinner from "./loadingMoreSpinner.style";

const LoadingMoreSpinner = () => {
	return <StyledLoadingMoreSpinner>loading...</StyledLoadingMoreSpinner>;
};

export default LoadingMoreSpinner;
